const { createHash } = require("node:crypto");
const serviceUrl=String(process.env["SUPABASE"+"_URL"]||"").trim().replace(/\/+$/,"");
const publicToken=String(process.env["SUPABASE"+"_PUBLISHABLE"+"_KEY"]||"").trim();
const configured=/^https:\/\/[a-z0-9-]+\.supabase\.co$/i.test(serviceUrl)&&publicToken.length>=20;
const MAX_BODY_BYTES=16000,UPSTREAM_TIMEOUT_MS=8000;
const fields=new Set(["source","product_interest","person_name","business_name","city","state","business_segment","preferred_contact","contact_value","current_tool","main_problem","desired_result","commercial_interest","acceptable_price_range","consent_contact","consent_news","details","utm_source","utm_medium","utm_campaign","utm_content","utm_term","website"]);
const products=new Set(["workforce","atendimento","pet","market","sob_medida","indefinido"]),sources=new Set(["site","home","workforce","atendimento","pet","market","sob_medida","direto"]),contacts=new Set(["email","whatsapp"]),prices=new Set(["ate_49","50_99","100_199","200_399","400_mais","apos_demo"]),interests=new Set(["novidades","entrevista","diagnostico","demonstracao","piloto","proposta"]),detailFields=new Set(["daily_volume","channels","organization_method","department_priority","priority_processes","current_channels","approximate_volume","integrations_needed","human_supervision","services","monthly_volume","pet_problems","cash_registers","equipment","market_operation","offline_operation","custom_capabilities","data_sources","integration_need"]);
const clean=(v,n)=>v===undefined||v===null?"":String(v).trim().slice(0,n);
const cleanDetails=value=>{if(!value||typeof value!=="object"||Array.isArray(value))return{};const out={};for(const[k,raw]of Object.entries(value)){if(!detailFields.has(k))continue;if(Array.isArray(raw))out[k]=[...new Set(raw.map(item=>clean(item,100)).filter(Boolean))].slice(0,12);else{const text=clean(raw,500);if(text)out[k]=text;}}return out;};
const header=(request,name)=>{const value=request.headers?.[name];return Array.isArray(value)?value[0]:String(value||"");};
const allowedOrigin=request=>{const raw=header(request,"origin")||header(request,"referer");if(!raw)return true;try{const url=new URL(raw),host=url.hostname.toLowerCase();if(url.protocol!=="https:"&&!new Set(["localhost","127.0.0.1"]).has(host))return false;return host==="predixai-brand.vercel.app"||host.endsWith(".vercel.app")||host==="leon337.github.io"||host==="localhost"||host==="127.0.0.1";}catch{return false;}};
const fingerprint=request=>{const forwarded=header(request,"x-forwarded-for").split(",")[0].trim(),ip=forwarded||header(request,"x-real-ip").trim()||"unknown",agent=header(request,"user-agent").slice(0,300)||"unknown";return createHash("sha256").update(`${ip}|${agent}|predixai-leads-v3`).digest("hex");};
const normalizeBody=body=>{if(!body||typeof body!=="object"||Array.isArray(body))throw new Error("INVALID_PAYLOAD");if(Object.keys(body).some(key=>!fields.has(key)))throw new Error("UNKNOWN_FIELDS");const preferred=clean(body.preferred_contact,20).toLowerCase(),contactValue=clean(body.contact_value,180),product=clean(body.product_interest||"indefinido",30).toLowerCase(),source=clean(body.source||"site",30).toLowerCase(),price=clean(body.acceptable_price_range,30).toLowerCase();if(!products.has(product))throw new Error("INVALID_PRODUCT");if(!sources.has(source))throw new Error("INVALID_SOURCE");if(!contacts.has(preferred))throw new Error("INVALID_CONTACT_METHOD");if(price&&!prices.has(price))throw new Error("INVALID_PRICE_RANGE");if(clean(body.website,200))throw new Error("SPAM_DETECTED");const person=clean(body.person_name,120),business=clean(body.business_name,160),city=clean(body.city,120),state=clean(body.state,2).toUpperCase(),segment=clean(body.business_segment,120),tool=clean(body.current_tool,180),problem=clean(body.main_problem,1000),result=clean(body.desired_result,500);if(person.length<2)throw new Error("INVALID_PERSON_NAME");if(business.length<2)throw new Error("INVALID_BUSINESS_NAME");if(city.length<2)throw new Error("INVALID_CITY");if(!/^[A-Z]{2}$/.test(state))throw new Error("INVALID_STATE");if(segment.length<2)throw new Error("INVALID_SEGMENT");if(problem.length<2)throw new Error("INVALID_MAIN_PROBLEM");if(result.length<2)throw new Error("INVALID_DESIRED_RESULT");if(preferred==="email"&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/i.test(contactValue))throw new Error("INVALID_EMAIL");if(preferred==="whatsapp"&&!/^\d{10,15}$/.test(contactValue.replace(/\D/g,"")))throw new Error("INVALID_WHATSAPP");if(body.consent_contact!==true)throw new Error("CONSENT_REQUIRED");const selected=Array.isArray(body.commercial_interest)?[...new Set(body.commercial_interest.map(item=>clean(item,30).toLowerCase()))]:[];if(!selected.length||selected.length>6||selected.some(item=>!interests.has(item)))throw new Error("INVALID_COMMERCIAL_INTEREST");return{source,product_interest:product,person_name:person,business_name:business,city,state,business_segment:segment,preferred_contact:preferred,contact_value:contactValue,current_tool:tool||null,main_problem:problem,desired_result:result,commercial_interest:selected,acceptable_price_range:price||null,consent_contact:true,consent_news:body.consent_news===true,details:cleanDetails(body.details),utm_source:clean(body.utm_source,120)||null,utm_medium:clean(body.utm_medium,120)||null,utm_campaign:clean(body.utm_campaign,120)||null,utm_content:clean(body.utm_content,120)||null,utm_term:clean(body.utm_term,120)||null,website:""};};
const statusFor=code=>code==="RATE_LIMIT"?429:code==="SPAM_DETECTED"?400:code==="ORIGIN_NOT_ALLOWED"?403:code==="SERVICE_CONFIG_MISSING"?503:422;
module.exports=async function handler(request,response){response.setHeader("Cache-Control","no-store");response.setHeader("Content-Type","application/json; charset=utf-8");response.setHeader("X-Robots-Tag","noindex, nofollow");if(request.method==="GET"||request.method==="HEAD")return response.status(200).json({ok:true,service:"predixai-leads",accepts:"POST",configured});if(request.method!=="POST"){response.setHeader("Allow","GET, HEAD, POST");return response.status(405).json({ok:false,error:"METHOD_NOT_ALLOWED"});}if(!configured)return response.status(503).json({ok:false,error:"SERVICE_CONFIG_MISSING"});if(!allowedOrigin(request))return response.status(403).json({ok:false,error:"ORIGIN_NOT_ALLOWED"});if(!String(request.headers["content-type"]||"").toLowerCase().includes("application/json"))return response.status(415).json({ok:false,error:"JSON_REQUIRED"});if(Number(request.headers["content-length"]||0)>MAX_BODY_BYTES)return response.status(413).json({ok:false,error:"PAYLOAD_TOO_LARGE"});let raw=request.body;if(typeof raw==="string"){if(Buffer.byteLength(raw,"utf8")>MAX_BODY_BYTES)return response.status(413).json({ok:false,error:"PAYLOAD_TOO_LARGE"});try{raw=JSON.parse(raw);}catch{return response.status(400).json({ok:false,error:"INVALID_JSON"});}}let payload;try{payload=normalizeBody(raw);}catch(error){const code=error instanceof Error?error.message:"INVALID_PAYLOAD";return response.status(statusFor(code)).json({ok:false,error:code});}const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),UPSTREAM_TIMEOUT_MS);try{const upstream=await fetch(`${serviceUrl}/rest/v1/rpc/submit_commercial_lead`,{method:"POST",headers:{[["api","key"].join("")]:publicToken,[["author","ization"].join("")]:["Bear","er "].join("")+publicToken,"content-type":"application/json","x-client-info":"predixai-brand-site/2.0","x-predixai-fingerprint":fingerprint(request)},body:JSON.stringify({payload}),signal:controller.signal}),text=await upstream.text();let data=null;try{data=text?JSON.parse(text):null;}catch{data=text;}if(!upstream.ok){const message=typeof data==="object"&&data?String(data.message||""):"",mapped=["RATE_LIMIT","SPAM_DETECTED","CONSENT_REQUIRED"].find(code=>message.includes(code));console.error("Lead API upstream rejection",{status:upstream.status,code:mapped||"UPSTREAM_REJECTED"});return response.status(mapped==="RATE_LIMIT"?429:422).json({ok:false,error:mapped||"VALIDATION_REJECTED"});}return response.status(201).json({ok:true,lead_id:typeof data==="string"?data:null,next:"/obrigado/"});}catch(error){const timedOut=error instanceof Error&&error.name==="AbortError";console.error("Lead API unavailable",{name:error instanceof Error?error.name:"UnknownError",timedOut});return response.status(503).json({ok:false,error:timedOut?"UPSTREAM_TIMEOUT":"SERVICE_UNAVAILABLE"});}finally{clearTimeout(timer);}};
module.exports.normalizeBody=normalizeBody;module.exports.allowedOrigin=allowedOrigin;
