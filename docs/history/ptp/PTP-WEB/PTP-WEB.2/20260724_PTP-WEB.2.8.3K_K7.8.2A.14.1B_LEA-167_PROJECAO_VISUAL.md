# PTP-WEB.2.8.3K.7.8.2A.14.1B — Projeção Visual da Configuração Efetiva

## 1. Identificação

- Linear: `LEA-167`
- Data: `2026-07-24`
- Status: `PASS_AUTOMATED_PENDING_HUMAN_VISUAL_VALIDATION`
- Base: `acaa9f5cb6fa57c8329a8bd174fef47fcbfa6e94`
- HEAD técnico validado: `cc8cd39ed659ac5479fe4f4d55d2deda49155a7c`
- Branch: `ptp-web-2-workforce-effective-projection-r1`
- PR: `#11`, draft e sem merge
- Relatório: `reports/20260724_PTP-WEB.2.8.3K_K7.8.2A.14.1B_LEA-167_VISUAL_PROJECTION.txt`

## 2. Objetivo

Fazer as telas de recomendação, configuração e revisão apresentarem os valores derivados pela `EffectiveAgentConfig`, preservando o criador genérico e sem criar uma nova fonte persistida.

## 3. Contexto

A LEA-166 entregou o contrato determinístico `buildEffectiveAgentConfig()`, mas a página ainda não carregava esse contrato e as telas continuavam lendo diretamente o catálogo e o estado genérico. Isso permitia divergência entre personalizações do pacote e a apresentação visual.

## 4. Arquivos afetados

- `assets/js/workforce-package-projection.js` — novo módulo de projeção em memória;
- `scripts/validate_workforce_package_projection.js` — novo validador automatizado;
- `funcionario-ia-gratis/index.html` — carregamento ordenado do contrato e da projeção;
- `.github/workflows/workforce-package-builder-quality.yml` — gate de sintaxe e contrato da LEA-167.

Não alterados:

- API;
- Supabase;
- `vercel.json`;
- materialização do prompt da LEA-168;
- catálogo genérico;
- produção.

## 5. Execução

A camada criada:

1. exige pacote ativo e `READY`;
2. constrói a `EffectiveAgentConfig` em memória;
3. invalida cache quando documento ou customização mudam;
4. projeta nome, apresentação, empresa, tom e tamanho de resposta;
5. mantém o seletor genérico de tom oculto, sincronizado e restaurável;
6. preserva o campo Segmento e adiciona Empresa separadamente;
7. bloqueia botões de avanço quando a projeção falha;
8. restaura o comportamento genérico fora do modo pacote.

## 6. Revisão crítica

### RC pré-implementação

`PASS_WITH_EXECUTION_ADJUSTMENT`.

Decisão: evitar reconstrução assíncrona dentro das funções internas de renderização; usar módulo independente com cache runtime e observação controlada da interface.

### RC pós-implementação

Foram encontrados e corrigidos três pontos:

- empresa não pode substituir semanticamente Segmento;
- estado `FAILED` deve bloquear ações, não apenas mostrar aviso;
- valor bruto do tom deve sincronizar o controle genérico oculto.

## 7. Validação

```txt
WORKFLOW_PACKAGE_BUILDER_RUN=30067382082
WORKFLOW_PACKAGE_BUILDER_RESULT=SUCCESS
WORKFLOW_WORKFORCE_RUN=30067382056
WORKFLOW_WORKFORCE_RESULT=SUCCESS
VERCEL_PREVIEW=READY
VERCEL_DEPLOYMENT=HGGnArvXWAoidgz6eUEqE8BPNcqK
```

Gates automatizados:

```txt
EFFECTIVE_CONFIG_SCRIPT_LOADED=PASS
PACKAGE_READY_REQUIRED=PASS
PACKAGE_FAILURE_FAIL_CLOSED=PASS
FAIL_CLOSED_ACTION_BLOCK=PASS
CUSTOM_EMPLOYEE_NAME_PROJECTED=PASS
CUSTOM_COMPANY_NAME_PROJECTED=PASS
CUSTOM_EMPLOYEE_PRESENTATION_PROJECTED=PASS
CUSTOM_TONE_PROJECTED=PASS
CUSTOM_TONE_RAW_SYNC=PASS
CUSTOM_RESPONSE_LENGTH_PROJECTED=PASS
SEGMENT_SEMANTICS_PRESERVED=PASS
GENERIC_PATH_PRESERVED=PASS
LEA_168_STARTED=NO
```

## 8. Resultado

```txt
LEA_167_AUTOMATED_IMPLEMENTATION=PASS
LEA_167_FINAL=NO
PR_11=DRAFT
PR_11_MERGE=NO
PR_10_MERGE=NO
PRODUCTION_CHANGED=NO
```

## 9. Pendências

A validação humana ainda deve comprovar:

- viewport de 360 px;
- viewport de 390 px;
- desktop;
- pacote sem personalização;
- Sophia + Clínica Ciame;
- tom e tamanho personalizados;
- campo opcional omitido;
- retorno ao criador genérico;
- recarregamento da mesma aba.

Depois, executar revisão crítica independente final.

## 10. Próxima ação

Executar validação visual humana no Preview do PR #11. Não executar merge e não iniciar a LEA-168.
